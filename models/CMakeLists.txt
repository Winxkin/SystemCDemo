add_subdirectory(Adder)
add_subdirectory(Counter)
add_subdirectory(Uart)


#add_library(models)
#target_link_libraries(models Adder Counter Uart)
#target_include_directories(models PUBLIC ${CMAKE_CURRENT_SOURCE_DIR})